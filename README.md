# Cutlery

This extension adds a cutlery yes/no option into the checkout of your TastyIgniter install.

Just add the component to the checkout page, and add `@component('cutleryField')` where you want it to appear.